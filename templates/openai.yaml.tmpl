interface:
  display_name: "{{DISPLAY}}"
  short_description: "{{SUMMARY}}"
  default_prompt: >-
    Use ${{NAME}} for this task. Follow SKILL.md. Prefer bundled scripts
    over regenerating them.
policy:
  allow_implicit_invocation: true
